models: 
    -
          - Input(shape=(256; 256; 3))
          - Dense(3, activation = 'softmax')
    -
          - Input(shape=(256; 256; 3))
          - Dense(3, activation = 'softmax')
    -
          - Input(shape=(256; 256; 3))
          - Dense(3, activation = 'softmax')