# COMMON
scoring: accuracy
image_size: 256
batch_size: 32

# MODEL TRAINING
model_trainer: 
      is_first_time: f # t or f
      model_name: CONVNET_1
      epochs: 52
      callbacks: 
            - EarlyStopping(monitor='val_accuracy', patience=2, min_delta=1)
      layers: 
            # data augmentation 
            - RandomFlip(mode='horizontal_and_vertical')
            - RandomRotation(factor=0.2)
            - RandomZoom(height_factor=0.2)
            # - RandomBrightness(factor=0.2)
            # - RandomGaussianBlur(kernel_size=3, factor=(0.0, 1.0))
            # - RandomContrast(factor=0.2)
            # - RandomHue(factor=0.08) 
            # - RandomSaturation(factor=0.3)

            # rescale 
            - Rescaling(scale = 0.00392) # 1 / 255

            # Convnet 
            - Conv2D(filters=32, kernel_size=3, activation='relu')
            - MaxPooling2D(pool_size=2)

            # Classifier
            - Flatten()
            - Dense(units=  64, activation='relu')
            - Dense(units=3, activation='softmax')

      optimizer: RMSprop()
      loss: sparse_categorical_crossentropy
      metrics: 
            - accuracy


# MODEL EVALUATION
evaluated_model_name: XGB_1

# MONITOR PLOT
monitor_plotter:
      monitor_plot_html_path: "artifacts/monitor_plot.html"
      target_val_value: 97
      max_val_value: 100
      dtick_y_value: 5
